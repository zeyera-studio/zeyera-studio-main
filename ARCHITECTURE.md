# 🏗️ Architecture Overview - Zeyera Studio

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │           React Application (SPA)                      │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │   Navbar     │  │  Hero/Home   │  │  Admin Panel │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │ Movies Page  │  │  TV Series   │  │ Auth Modal   │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  │                                                         │    │
│  │              Managed by React Router                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              AuthContext (State Management)            │    │
│  │  • User Session  • Profile Data  • Auth Methods       │    │
│  └────────────────────────────────────────────────────────┘    │
└──────────────────┬──────────────────┬─────────────────┬─────────┘
                   │                  │                 │
                   ▼                  ▼                 ▼
         ┌─────────────────┐  ┌──────────────┐  ┌──────────────┐
         │   SUPABASE      │  │  GEMINI AI   │  │   VERCEL     │
         │   (Backend)     │  │   (AI API)   │  │  (Hosting)   │
         └─────────────────┘  └──────────────┘  └──────────────┘
         │                    │                 │
         │ • PostgreSQL DB    │ • Content AI    │ • CDN
         │ • Auth System      │ • Search AI     │ • Edge Network
         │ • REST API         │ • Recommend.    │ • Auto Deploy
         │ • Real-time        │                 │ • SSL/HTTPS
         └────────────────────┘                 └──────────────┘
```

---

## Data Flow Architecture

### 1. User Authentication Flow

```
┌──────┐     Sign Up/Login      ┌──────────────┐     Verify JWT     ┌──────────┐
│ User │ ────────────────────► │  Supabase    │ ─────────────────► │ Backend  │
└──────┘                        │  Auth API    │                     │ Database │
   ▲                            └──────────────┘                     └──────────┘
   │                                    │                                  │
   │                                    ▼                                  ▼
   │                            ┌──────────────┐                     ┌──────────┐
   │◄───────────────────────────┤ JWT Token    │◄────────────────────┤ Profile  │
   │      Store in Browser      │   Created    │     Profile Data    │ Created  │
   │                            └──────────────┘                     └──────────┘
```

### 2. AI Recommendation Flow

```
User Query
    │
    ▼
┌─────────────────┐
│ Search Input    │
│ "action movies" │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ geminiService   │ ──────► Validate API Key
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Google Gemini   │ ──────► Process Natural Language
│ AI API          │         Analyze Context
└─────────────────┘         Generate Recommendations
    │
    ▼
┌─────────────────┐
│ JSON Response   │ ──────► Parse Data
│ 6 Movies/Shows  │         Add Images
└─────────────────┘         Format for Display
    │
    ▼
┌─────────────────┐
│ Display Results │
│ in Grid Layout  │
└─────────────────┘
```

### 3. Admin User Management Flow

```
Admin Action (Change Role)
    │
    ▼
┌──────────────────┐
│ AdminDashboard   │
│ Component        │
└──────────────────┘
    │
    ▼
┌──────────────────┐
│ Optimistic UI    │ ──────► Update UI immediately
│ Update           │         (Better UX)
└──────────────────┘
    │
    ▼
┌──────────────────┐
│ Supabase API     │ ──────► UPDATE profiles
│ Request          │         SET role = 'admin'
└──────────────────┘         WHERE id = user_id
    │
    ├──► Success ──────► Keep UI changes
    │
    └──► Error ────────► Revert UI changes
                         Show error message
```

---

## Component Hierarchy

```
App
 ├── AuthProvider (Context)
 │    └── AppContent
 │         ├── Navbar
 │         │    ├── Search Bar
 │         │    ├── Navigation Links
 │         │    └── Auth Button
 │         │
 │         └── Main Content
 │              ├── Home Page
 │              │    ├── Hero Section
 │              │    ├── SplitShowcase
 │              │    │    └── Content Grid
 │              │    ├── ContactSection
 │              │    └── Footer
 │              │
 │              ├── Movies Page
 │              │    └── Movie Grid
 │              │
 │              ├── TV Series Page
 │              │    └── TV Grid
 │              │
 │              └── Admin Dashboard
 │                   ├── Sidebar Navigation
 │                   ├── Overview Tab
 │                   │    └── Statistics Cards
 │                   ├── Users Tab
 │                   │    ├── User Creation Form
 │                   │    └── User Management Table
 │                   └── Content Tab
```

---

## Security Architecture

### Row-Level Security (RLS) Policies

```
┌────────────────────────────────────────────────────┐
│              SUPABASE DATABASE                     │
│                                                     │
│  ┌──────────────────────────────────────────┐     │
│  │         profiles TABLE                   │     │
│  │  ┌────────────────────────────────┐      │     │
│  │  │ RLS Policy Layer              │      │     │
│  │  │                                │      │     │
│  │  │  IF auth.uid() = profile.id   │      │     │
│  │  │    → Allow SELECT, UPDATE     │      │     │
│  │  │                                │      │     │
│  │  │  IF user.role = 'admin'       │      │     │
│  │  │    → Allow ALL operations     │      │     │
│  │  │                                │      │     │
│  │  │  ELSE                          │      │     │
│  │  │    → DENY                      │      │     │
│  │  └────────────────────────────────┘      │     │
│  │                                           │     │
│  │  ┌─────────┬──────────┬──────┬────────┐  │     │
│  │  │   id    │  email   │ role │  ...   │  │     │
│  │  ├─────────┼──────────┼──────┼────────┤  │     │
│  │  │  uuid1  │ a@x.com  │ user │  ...   │  │     │
│  │  │  uuid2  │ b@x.com  │admin │  ...   │  │     │
│  │  └─────────┴──────────┴──────┴────────┘  │     │
│  └──────────────────────────────────────────┘     │
└────────────────────────────────────────────────────┘
```

### Authentication Token Flow

```
Login Success
    │
    ▼
┌──────────────────┐
│ JWT Token        │  Contains:
│ Generated        │  • User ID
└──────────────────┘  • Email
    │                 • Role (in metadata)
    │                 • Expiry (1 hour default)
    ▼
┌──────────────────┐
│ Store in:        │
│ • LocalStorage   │  Persistent across sessions
│ • SessionStorage │  Clear on browser close
│ • Cookie         │  HTTP-only option
└──────────────────┘
    │
    ▼
┌──────────────────┐
│ Every API Call   │  Header:
│ Includes Token   │  Authorization: Bearer <JWT>
└──────────────────┘
    │
    ▼
┌──────────────────┐
│ Supabase         │  Validates:
│ Validates        │  • Signature
│ Token            │  • Expiry
└──────────────────┘  • Issuer
    │
    ├──► Valid ────► Allow Request
    │
    └──► Invalid ──► 401 Unauthorized
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────────────┐
│         PRESENTATION LAYER (Frontend)           │
├─────────────────────────────────────────────────┤
│  React 18.2  │  TypeScript 5.2  │  Tailwind 3.4│
│  Lucide Icons│  React Context   │  Custom CSS  │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         BUILD LAYER (Development)               │
├─────────────────────────────────────────────────┤
│  Vite 5.1    │  PostCSS        │  Autoprefixer │
│  TypeScript  │  ES Modules     │  Tree-shaking │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         SERVICE LAYER (APIs)                    │
├─────────────────────────────────────────────────┤
│  Supabase Client  │  Gemini AI Client          │
│  @supabase/js     │  @google/generative-ai     │
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         BACKEND LAYER (BaaS)                    │
├─────────────────────────────────────────────────┤
│  Supabase (PostgreSQL, Auth, Storage, APIs)    │
│  Google Gemini AI (Natural Language Processing)│
└─────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│         DEPLOYMENT LAYER (Hosting)              │
├─────────────────────────────────────────────────┤
│  Vercel Edge Network  │  CDN  │  SSL/HTTPS     │
│  Automatic CI/CD      │  Git Integration       │
└─────────────────────────────────────────────────┘
```

---

## Why This Architecture?

### ✅ **Separation of Concerns**
- Frontend focuses on UI/UX
- Backend handles data and auth
- AI service handles intelligence

### ✅ **Scalability**
- Vercel's edge network scales automatically
- Supabase handles millions of requests
- Stateless architecture (no server state)

### ✅ **Security**
- Database-level RLS policies
- JWT-based authentication
- Environment variable isolation
- HTTPS everywhere

### ✅ **Developer Experience**
- Hot Module Replacement (HMR) for instant feedback
- TypeScript for type safety
- Auto-generated API types from database
- Simple deployment with Git push

### ✅ **Performance**
- Static site generation (SSG) where possible
- Code splitting and lazy loading
- Edge caching via CDN
- Optimized build with Vite

### ✅ **Maintainability**
- Component-based architecture
- Reusable hooks and contexts
- Clear file structure
- Comprehensive type definitions

---

## Database Schema & Relationships

```
┌─────────────────────┐
│   auth.users        │  (Supabase managed)
│─────────────────────│
│ id (UUID) PK        │
│ email               │
│ encrypted_password  │
│ email_confirmed_at  │
│ created_at          │
└──────────┬──────────┘
           │
           │ 1:1 relationship
           │ ON DELETE CASCADE
           │
           ▼
┌─────────────────────┐
│  public.profiles    │  (Custom table)
│─────────────────────│
│ id (UUID) PK, FK    │───► References auth.users(id)
│ email (TEXT)        │
│ username (TEXT)     │
│ role (TEXT)         │───► 'user' | 'admin'
│ created_at          │
└─────────────────────┘
```

### Automatic Profile Creation Trigger

```sql
-- When a new user signs up in auth.users
-- Automatically create a matching profile row

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW
  EXECUTE FUNCTION handle_new_user();
```

This ensures every authenticated user has a profile record automatically.

---

## Deployment Pipeline

```
Developer's Machine
    │
    │  git commit && git push
    ▼
┌────────────────┐
│  GitHub Repo   │
└────────────────┘
    │
    │  Webhook trigger
    ▼
┌────────────────┐
│ Vercel Build   │
│   Platform     │
└────────────────┘
    │
    │  npm install && npm run build
    ▼
┌────────────────┐
│  Build Assets  │
│  (dist/)       │
└────────────────┘
    │
    │  Deploy to edge network
    ▼
┌────────────────┐
│ Vercel CDN     │  (Global distribution)
│ Edge Locations │
└────────────────┘
    │
    ▼
   Users access from anywhere
```

**Build Time:** ~1-2 minutes  
**Deploy Time:** ~30 seconds  
**Total Time:** ~2-3 minutes from push to live

---

**Questions?** Refer to README.md for detailed feature explanations.

